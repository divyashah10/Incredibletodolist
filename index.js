import { AppRegistry } from 'react-native';
import App from './do {
    App
} while (condition);';
import { name as IncredibleTodoListApp } from './app.json';

AppRegistry.registerComponent(IncredibleTodoListApp, () => App);